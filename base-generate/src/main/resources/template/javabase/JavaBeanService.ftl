package ${JavaBeanServicePath};

import ${JavaBeanPath}.${poName};
import com.application.base.core.datasource.service.StrutsBaseService;

/**
 * ${poName}实体
 * 
 * @author 系统生成
 *
 */
public interface ${poName}Service extends StrutsBaseService<${poName}>{
	
	//可以填写自己需要的 Service 方法.

}